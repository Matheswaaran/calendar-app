import React from "react";
import "./index.css";
import Calendar from "../../components/calendar";

const Home = (props) => {
  return (
    <div className="home-container">
      <Calendar />
    </div>
  );
};

export default Home;
